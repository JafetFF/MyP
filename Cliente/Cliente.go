package main

type Cliente struct {
     direccion string
}

// Función que regresa un nuevo cliente
func NuevoCliente(direccion string) *Cliente {
     // Regresa un nuevo cliente
}

// Función que conecta al cliente al servidor
func Conecta() {
     // Se conecta al servidor
}
